# Multi-Model Tier Configuration
# Balanced: Opus for coordination/architecture, Sonnet for capable workhorse tasks,
# Haiku for mechanical/validation. Optimized for token efficiency.

default_model: sonnet

# ─── Tier Definitions ───────────────────────────────────────────────

tiers:
  opus:
    model_id: claude-opus-4-6
    use_for:
      - Orchestration, planning, architecture
      - Complex multi-step coordination
      - Recursive exploration controllers
      - planning-lead in orchestrate jobs (wrong plan = everything downstream wrong)

  sonnet:
    model_id: claude-sonnet-4-6
    max_turns_default: 25  # Cap for research/analysis agents; builder=20
    use_for:
      - Research and analysis (context-first, 25 turns max)
      - Code generation and implementation (20 turns max)
      - Code review and refactoring
      - Security analysis
      - Agent/skill generation
      - Codebase scouting and reporting
      - Critical analysis

  haiku:
    model_id: claude-haiku-4-5
    use_for:
      - Read-only validation (syntax checks, linting)
      - Fast file exploration (Explore agent)
      - Documentation scraping
      - Data formatting and mechanical transforms
      - Status reporting and TTS summaries

# ─── Agent Assignments ──────────────────────────────────────────────
# 4 Opus, 29 Sonnet, 4 Haiku (+ 1 Sonnet/Haiku dynamic: builder)
# Removed: rlm-root (now /rlm command), solve (protocols folded into orchestrator), engineering-lead (replaced by domain leads)

agent_tiers:
  opus:
    - orchestrator            # Multi-agent coordination and planning
    - project-architect       # Architecture design, agent ecosystem creation
    - planning-lead           # Wave planning lead — wrong plan = everything downstream wrong
    - po                      # Product Owner — vision alignment, lead routing, escalation handling

  sonnet:
    - critical-analyst        # Critical thinking, risk analysis
    - researcher              # Research and analysis
    - meta-agent              # Agent file generation
    - scout-report-suggest    # Codebase scouting
    - builder                 # Pure implementation — default Sonnet, Haiku for mechanical tasks
    - lead                    # Generic orchestration lead (fallback/base)
    - debugger                # Root cause analysis + fix plan writing
    - onboard-builder         # Wave 1 onboarding — generates project files from plan
    - onboard-planner         # Wave 0 onboarding — detects stack, writes plan
    - academic-researcher     # Academic paper search and citation graph traversal
    - code-researcher         # Cross-repo code pattern search via GitHub MCP
    # ── Domain-specific lead agents ──
    - frontend-lead           # Frontend/UI/UX domain spec + delegation
    - backend-lead            # Backend/business logic domain spec + delegation
    - api-lead                # API design + contracts domain spec + delegation
    - mobile-lead             # Mobile app domain spec + delegation
    - data-lead               # Data layer/DB domain spec + delegation
    - infra-lead              # Infrastructure/deploy domain spec + delegation
    - qa-lead                 # Test coverage planning + delegation
    - review-lead             # Code quality review planning + delegation
    - security-lead           # Threat modeling planning + delegation
    - debugging-lead          # Root cause analysis planning + delegation
    - architecture-lead       # System design + ADR planning + delegation
    - testing-lead            # Test strategy planning + delegation
    - refactoring-lead        # Refactor planning + delegation
    - release-lead            # Ship/deploy planning + delegation
    - docs-lead               # Documentation planning + delegation
    - performance-lead        # Perf profiling planning + delegation
    - design-lead             # UX/visual design planning + delegation
    - ceo-review-lead         # Strategic review planning + delegation
    - eng-review-lead         # Technical feasibility planning + delegation
    - pairing-lead            # Complex debugging via pair sessions + delegation

  haiku:
    - docs-scraper            # Documentation scraping (speed > depth)
    - validator               # Test running + pass/fail reporting
    - agent-watchdog          # Background anomaly detection
    - health-checker          # CAF health checks — mechanical, no reasoning needed

# ─── Skill Overrides ────────────────────────────────────────────────

skill_default_models:
  test-generator: sonnet
  code-review: sonnet
  refactoring-assistant: sonnet
  security-scanner: sonnet
  error-analyzer: sonnet
  knowledge-db: haiku     # Mechanical DB ops, speed matters

# ─── Decision Matrix ────────────────────────────────────────────────

decision_matrix:
  - condition: "Coordination, orchestration, or architecture decisions"
    model: opus
  - condition: "Code generation, analysis, review, or research"
    model: sonnet
  - condition: "Mechanical validation, exploration, or data formatting"
    model: haiku
  - condition: "Unknown complexity"
    model: sonnet  # Default to capable workhorse
