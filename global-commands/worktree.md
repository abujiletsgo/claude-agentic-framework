---
description: Manage git worktrees for parallel Claude Code sessions — create, list, remove, clean
argument-hint: "[new <branch> | list | remove <branch> | clean | status]"
---

# Worktree

$ARGUMENTS

Manage git worktrees so multiple Claude Code sessions can run on the same project simultaneously, each on its own branch and directory.

Delegate to the `worktree` skill with the arguments above.
