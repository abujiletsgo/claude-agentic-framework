# /orchestrate

Launch the unified orchestrator. You ARE the orchestrator — do not spawn a separate orchestrator agent.

**Immediately call:** `Skill(skill="orchestrate", args="<user's full message>")`

The skill protocol will load. Follow it directly: analyze → spawn researchers → synthesize → spawn builders → validate.

Never do work yourself. Always spawn parallel Agent() calls.
